// Stub: canvas mutation replay removed (fork-3 prune).
// The canvas recording/replay pipeline is unused in pinconsole.
// Selective screenshots are handled by a separate collector (visitor-sdk).
export default function canvasMutation(): void {
  // no-op
}
