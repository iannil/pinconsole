# 每日笔记 — 2026-06-20

> 不可变日志(流层)。今日发生的所有重要事件按时间顺序追加。
> 不要修改或删除已有条目。重要模式沉淀到 [`../MEMORY.md`](../MEMORY.md)。

---

## 跨日衔接(自 2026-06-19 23:30 ~ 2026-06-20 00:57)

承接 2026-06-19 的 1ai-b ~ 1ai-f 接口化重构链。详见 [`2026-06-19.md`](./2026-06-19.md) §"32:30 — 1ai-h SessionHandler 接口化 + replay 纯函数"——2026-06-19 跨日段已记录 1ai-g/1ai-h 完成细节。

本日新增工作(2026-06-20 00:57 之后):

### 00:57 — 1ai-e claim+chat 接口化完成

- commit `9d70c73`(docs sync) + `ccf9fca`(refactor) + `455ac13`(test)
- 3 新接口:`chatMessageLister` / `chatMessageCreator` / `claimSessionRepo`(claim_chat_interfaces.go 扩展)
- ClaimHandler / ChatHandler 字段重构:加 sessionRepo / chatLister / chatCreator 接口字段
- listMessages / getClaim / release / postClaim 4 个 happy path 测试 + 3 mock
- 覆盖率:claim 25%→78.1%、listMessages 57%→76.2%、api 包 31.8%→33.7%
- 详见 [`docs/reports/completed/2026-06-20-slice-1aie-claim-chat-interface-implementation.md`](../../docs/reports/completed/2026-06-20-slice-1aie-claim-chat-interface-implementation.md)

### 09:21 — 1ai-f CommandHandler 接口化(精简)+ postCommand 拒绝路径

- commit `b42009f`(refactor) + `1067ccd`(test) + `757e277`(docs sync)
- commandRepo 接口 + CommandHandler 字段重构
- 3 测试:Register/no-user 401/not-owner 403(拒绝路径)
- postCommand 0%→16%(拒绝路径)
- 详见 [`docs/reports/completed/2026-06-20-slice-1aif-command-handler-interface-implementation.md`](../../docs/reports/completed/2026-06-20-slice-1aif-command-handler-interface-implementation.md)

### 09:24 — gofmt 全量清债

- commit `51b0da9` — style: gofmt -w 全量格式化(struct field 对齐 + 缩进规范)
- 纯空格/对齐调整,无逻辑变更

### 09:29 — 1ai-g requireClaimOwnership 接口化 + ChatHandler 字段重构

- commit `2c186d0`(refactor) + `0f5f347`(test) + `a75c2f6`(docs sync)
- 继 1ai-f,完成 requireClaimOwnership 接口化(影响 chat.go postMessage + command.go postCommand + authz_test.go 5 处)
- authz.go requireClaimOwnership 改接受 (sessionRepo, redis) 接口参数
- chat.go ChatHandler 加 sessionRepo / redis / createMsg(chatMessageCreator)字段
- 4 新测试(Success/NotOwner/InvalidJSON/EmptyContent)+ 2 新 mock
- 覆盖率:chat.go postMessage 35%→85%、api 33.8%→34.6%
- Mutation KILLED:sender operator → visitor
- **无独立 impl 报告**(只同步 daily);实施记录见本日 §"09:29" 段

### 11:11 — 1ai-h SessionHandler 接口化 + initSession happy path + replay 纯函数测试

- commit `7af0807`(test) + `b748e43`(docs sync) + `08377d8`(fix flaky)
- SessionHandler 加 sessionRepo 字段(sessionInitRepo 接口)
- 4 个 initSession 测试(Success/MissingVisitorID/InvalidJSON/CreateVisitorError)
- 6 个 replay 纯函数测试(eventPayloadToMap/decodePayloadAsEvent/decodeRRWebEventsFromBlob/extractRRWebEventsFromPayload)
- initSession 0%→87%、decodePayloadAsEvent 0%→100%、eventPayloadToMap 0%→100%
- api 包覆盖 34.6% → 38.2%(commit message 自报;**当晚 docker 实测验证为 38.2%**,此前文档一度写"实测 28.7%"是本地无 docker 跑测试的虚假低数字)
- **无独立 impl 报告**(只同步 daily);实施记录见 2026-06-19 daily §"32:30 — 1ai-h"

### 11:13 — storage 时间窗口测试 sleep 10ms → 100ms 防 PG 时钟漂移 flaky

- commit `08377d8` — fix(1ai-h): storage 时间窗口测试 sleep 10ms → 100ms 防 PG 时钟漂移 flaky
- CI flaky 修复,无功能变更

### 1ag~1ai-h 累计

- **75 个新测试**(12 + 10 + 1 + 11 + 11 + 3 + 4 + 4 + 4)
- api 包覆盖 **20.0% → 38.2%**(commit 自报,docker 实测验证)
- storage 包覆盖 **20.1% → 57.6%**(1ai-b 自报,docker 实测验证)
- 15 个新测试文件 + 2 接口定义文件
- **2 个代码 fix** + **5 接口化重构**(AuthHandler + ClaimHandler/ChatHandler.listMessages + CommandHandler + requireClaimOwnership/ChatHandler.postMessage + SessionHandler)

> **注**:本段记录的覆盖率数字基于当时(1ai-h 完成时)commit message 自报。当晚 18:00 启动 docker 实测验证后确认与自报吻合(api 38.2% / storage 57.6%)。此前文档一度写"实测 28.7% / 1.5%"是本地无 docker 跑测试的虚假低数字,详见本日 §"18:00~ — 项目单元测试覆盖率评估"。

### 关键里程碑

- api 包覆盖首次破 35%(commit 自报 38.2%,docker 实测验证一致)
- 接口化模式 PoC 已在 5 个 handler 验证
- 1ag-1ai-h 累计 commit 数:30+

---

## 17:40~18:00 — marketing-monitor → pinconsole 全量重命名重构(5 commit)

### 动机

`marketing-monitor` 是项目早期命名,与最终发布名 `pinconsole` 不一致。已完成 Go module / pnpm scope / docker / DB / embeds / 文档 / CI / e2e 全量重命名。

### 5 步执行

| 步 | commit | 范围 |
|---|---|---|
| 1 | `f461b59` | Go module `github.com/iannil/marketing-monitor` → `github.com/iannil/pinconsole`(go.mod / go.sum / 所有 import) |
| 2 | `dbf631b` | pnpm scope `@marketing-monitor/*` → `@pinconsole/*`(admin / visitor-sdk / e2e / packages 的 package.json + import 路径) |
| 3 | `d3d5f03` | docker-compose.yml 服务名/容器名 + DB schema 默认值(MINIO_BUCKET / database env)+ Go embed 路径重建 |
| 4 | `ea271d7` | 当前活跃文档(project-status / MEMORY / daily / standards / CLAUDE / PLAN / README)+ CI workflows / ops 脚本全量改 pinconsole |
| 5 | `234fa06` | e2e DB 连接配置 + visitor-sdk package.json description 清理 + 全量 verify(`go build` + `go test` + `pnpm typecheck` + `pnpm test`) |

### 历史快照处理

- `docs/reports/completed/` 22 份报告中的 `@marketing-monitor/*` / `github.com/iannil/marketing-monitor` / `MINIO_BUCKET=marketing-monitor` 字面量已批量替换为 `@pinconsole/*` 对应值(类别 1:命令字面量)
- `e2e/tests/*.ts` 4 份文件中的"1r 切片后 SDK 不再输出 marketing-monitor 字面量"类**历史断言保持原样**(类别 2:历史语义,改了会让断言失效)
- `docs/reports/completed/2026-06-17-slice-1a-spec.md` 等仓库根目录树快照中的 `marketing-monitor/` **保留原样**(类别 3:目录树快照)

### verify

- `go build ./...` ✅
- `go test ./...` ✅(全绿)
- `pnpm typecheck && pnpm test` ✅
- `grep -rln "marketing-monitor" server/ admin/ visitor-sdk/` ✅(代码 0 残留,只有 e2e 历史注释)

---

## 17:56~ 当下 — 文档整理 + 进展梳理 + 冗余清理

### 用户请求

1. 更新当前进度到 /docs 文件夹 + 更新记忆
2. 处理/移动/调整 /docs 文档到正确位置(归档/完成/计划)
3. 梳理项目实际进展,完整详细整理到文档(LLM 友好)
4. 梳理当前项目中冗余/过期/错误/无效的业务/代码/逻辑/脚本/测试

### 关键决策(grill-me 3 问 3 答)

- **START.md 不存在但被 5 份文档引用为"产品事实来源"** → 移除所有引用,统一指向 PLAN.md
- **22 份历史报告 + 4 份 e2e 测试中 marketing-monitor 残留** → 批量更新为 pinconsole(区分历史快照语义)
- **进展文档形态** → 增强现有 docs/project-status.md 作为单一权威(不新增 v1 终态报告)

### 发现的系统性问题

1. project-status.md §2/§5 badge 计数冲突(§2 写 21/10,§5 末尾写 23/9)→ 统一为 23/9/0
2. project-status.md 顶部和 §5 表格都停在 1ai-f,缺 1ai-g/1ai-h
3. README.md 严重落后(停留在 1a-1z + e2e + 5 followup 时代)
4. PLAN.md §7 v1 切片表只到 1j,未反映已完成的 1k-1z 加固阶段和 1aa-1ai-h 测试深化阶段
5. memory/daily/2026-06-20.md 不存在(违反"每次交互确认当日记忆存在"约定)
6. IMPLEMENTATION_PLAN.md 严重过期(停在 1aa/1ab/1ae/1af,缺 1ac-1ad/1ag-1ai-h;表里 31 切片全标 🟢 ×33 是虚标;还在引用 START.md)→ 决策**删除**

### 实施阶段

- 阶段 A:修正 docs/project-status.md(单一事实源)
- 阶段 B:辐射派生文档(README + MEMORY + 本 daily)
- 阶段 C:PLAN.md §7/§8 重组(追加加固阶段 + 测试深化阶段子表)
- 阶段 D:批量清理(START.md 引用 / marketing-monitor 残留 / IMPLEMENTATION_PLAN.md 删除 / audits 状态行 / docs/README 切片索引补全)
- 验证:grep START.md 残留 + badge 计数一致 + 切片完整 + marketing-monitor 范围 + daily 流连续 + git status 文件数

### 计划文件

[`~/.claude/plans/1-docs-imperative-spring.md`](/Users/rong.zhu/.claude/plans/1-docs-imperative-spring.md)

---

## 18:00~ — 项目单元测试覆盖率评估

### 用户请求

评估项目单元测试覆盖率(纯评估,不动代码)。

### 关键决策(grill-me 3 问 3 答)

- **产出形态**:独立 audit 报告 `docs/audits/2026-06-20-coverage-assessment.md`(保留 project-status.md §2.1 简表)
- **Storage 实测**:启动 docker-compose 跑真实集成测试覆盖率(验证 1ai/1ai-b 声称的 20-57%)
- **范围**:纯评估(识别盲区 + 量化差距 + 改进建议 backlog),不动代码

### 阶段 A:启动 docker + 实测覆盖率

#### A1 docker 基础设施

- 启动 docker compose up -d postgres redis minio(3 个容器 healthy)
- 发现 `.env` / `.env.example` 仍含 `PG_DB=marketing_monitor` / `MINIO_BUCKET=marketing-monitor`(rename 重构遗漏,详见 audit §6.5)
- docker 实际起的数据库名是 `marketing_monitor`(因为 .env 优先于 docker-compose 默认值 `pinconsole`)
- 测试代码 `internal/storage/erasure_test.go:34` 硬编码 `localhost:5432/pinconsole`,与 docker 实际不匹配
- workaround:手动 `CREATE DATABASE pinconsole` + 应用 migrations → 测试可连

#### A2 Go 全包覆盖率(docker 环境,2026-06-20 实测)

| 包 | 覆盖率 | 评级 |
|---|---|---|
| config | 98.0% | 🟢 |
| privacy | 95.0% | 🟢 |
| proto | 88.9% | 🟢 |
| antiscrape | 86.7% | 🟢 |
| observability | 83.3% | 🟢 |
| logging | 79.6% | 🟢 |
| hub | 73.0% | 🟢 |
| storage | 57.6% | 🟡 |
| recording | 48.0% | 🟡 |
| api | 38.2% | 🟡 |
| cmd/server | 4.9% | 🔴 |

加权整体 ~65%。

#### A3 前端测试用例数(grep 实测)

- admin: 82 用例(9 测试文件 / 1471 行)
- visitor-sdk: 68 用例(7 测试文件 / 1292 行)
- 总计 150(1aa 报告声称 112,**低估 34%**)
- vitest coverage **完全未配**(provider/thresholds/reporter 全缺),行覆盖率无法量化

#### A4 e2e 场景

- 19 spec × 91 test cases
- 1a-1l 主干全覆盖,1m-1z 部分通过专项 spec 覆盖,1aa-1ai-h 测试深化无专属 e2e

### 阶段 B:撰写 audit 报告

新建 `docs/audits/2026-06-20-coverage-assessment.md`(8 节结构 + disclaimer):

- §1 执行摘要(整体 🟡,Top 3 风险)
- §2 Go 逐包覆盖率(docker 实测)+ storage 内部分解(repo 高,适配器 0%)
- §3 前端用例数(1aa 低估对账)
- §4 e2e 场景矩阵(19 spec × 91 test,沿用 T0~T3 rubric)
- §5 关键盲区 Top 20(isURLAllowed / RegisterMe / DeleteVisitor / 适配器全 0% / SDK 主入口 0%)
- §6 历史虚标修正(6/8 虚标,2/8 真实)+ 附带发现:.env rename 残留
- §7 改进 backlog(CI 门槛 / vitest coverage / 补测 Top 10 / 文档同步)
- §8 验证方法(可重复跑的命令)

### 阶段 C:同步派生文档

- `docs/project-status.md` §2.1 改为完整逐包表(11 包),修正 storage 1.5%→57.6%、api 28.7%→38.2%、e2e 20→19,加 audit 链接
- `docs/README.md` audits 行追加 coverage-assessment 链接
- 本 daily 追加本段记录

### 关键发现

1. **6 处历史虚标**:project-status §2.1 数据全采自本地无 docker 环境,严重低估真实覆盖率
2. **2 处真实**:commit `b748e43` 自报"api 38.2%"和 daily 写"storage 57.6%" 都对
3. **附带发现**:.env rename 残留(`PG_DB=marketing_monitor`),导致默认 docker compose up 后测试连不上 → 显示 1.5% 假低覆盖率
4. **关键盲区**:isURLAllowed(URL 白名单核心)0%、所有 storage 适配器 0%、SDK 主入口 index.ts 2371 行 0%

### audit 报告

[`docs/audits/2026-06-20-coverage-assessment.md`](../docs/audits/2026-06-20-coverage-assessment.md)

---

## 19:00~ — 切片 0:.env rename + docker 自动建库(覆盖率提升前置)

### 用户请求

"项目单元测试覆盖率不低于90%" — 多切片渐进执行,Go phase 完成后停下来汇报。

### 切片 0 范围

修 `.env` / `.env.example` 中 rename 重构遗漏的 `PG_DB=marketing_monitor` / `MINIO_BUCKET=marketing-monitor` / `ADMIN_EMAIL=admin@marketing-monitor.local`,让 docker 自动建 pinconsole 数据库与测试代码硬编码一致。

### 实施

- `.env` 3 处 + `.env.example` 4 处改为 pinconsole
- README "快速开始" 改用 `make docker-up` 等(发现现有 Makefile 183 行已完善,**无需新建**)
- README 加"老开发者迁移"段(marketing_monitor volume 数据需手动迁移)
- `docker compose down -v && up -d` → PG 自动建 pinconsole 数据库 ✅
- 手动 psql 应用 5 个 migrations → 7 张表建成 ✅
- `make test-go` 全绿 ✅

### 关键修正

- plan 阶段判断"Makefile 不存在"是错的——实测 `make help` 显示 30+ target 已就绪
- 修正 spec/impl 文档反映"用现有 Makefile,不新建"
- 之前文档一度写"实测 1.5%"是 .env 残留导致的假低数字,本切片修了 .env 后 storage 实测 57.6%

### 验证

- ✅ `docker compose down -v` 后 PG 自动建 pinconsole 数据库(postgres:16-alpine 标准行为)
- ✅ 7 张表通过手动 psql 应用 migrations 建成
- ✅ `make test-go` 全绿(11 个 internal 包 + cmd/server + migrations)
- ✅ 覆盖率与 audit §2 一致:storage 57.6% / api 38.2% / antiscrape 86.7% / recording 48.0% / config 98% / privacy 95%

### 后续切片解锁

- **Go-4**(storage 适配器):依赖真实 PG/Redis/MinIO,本切片前必失败
- **Go-5**(recording):同上
- **Go-6**(api):间接依赖

Go-1/Go-2/Go-3 不依赖本切片,可并行启动。

### 切片文档

- [spec](../docs/reports/completed/2026-06-20-slice-0-env-rename-fix-spec.md) + [implementation](../docs/reports/completed/2026-06-20-slice-0-env-rename-fix-implementation.md)

### 工时

实际 ~1h(预估 2h)。

---

## 21:00~ — Go-1:proto + antiscrape + observability 小包冲刺

### 范围

3 个 Go 包从 79-89% 提升到 ≥90%。

### 实施

- `internal/proto/envelope_extra_test.go`(7 测试):DecodePayload 边界(marshal chan 失败、原始类型 payload、nil dst)
- `internal/antiscrape/coverage_extra_test.go`(10 测试):DefaultRateLimitConfig、IsSessionFlagged(Redis 集成)、extractXY 边界、Observe 边界
- `internal/observability/lifecycle_extra_test.go`(7 测试):LifecycleWithArgs、LogPoint/LogExternalCall 多 extras 路径

### 关键修正(实施过程发现)

1. **proto Decode 签名误读**:Go 单参数返回值 vs 双参数写入式;`Decode(b) → (Envelope, error)` 不是 `Decode(b, &env)`
2. **observability EventType 大小写**:常量 `EventFunctionStart` 值是 `"Function_Start"`(PascalCase),不是 `"function_start"`(小写)
3. **proto DecodePayload 75% 真正 gap**:Marshal 失败路径,用 chan payload 触发(不是 nil dst)

### 实测覆盖率

| 包 | 切片前 | 切片后 |
|---|---|---|
| proto | 88.9% | **100.0%** 🟢 |
| antiscrape | 86.7% | **95.9%** 🟢 |
| observability | 83.3% | **91.7%** 🟢 |

### 文档

- [spec](../docs/reports/completed/2026-06-20-slice-go1-coverage-misc-spec.md) + [implementation](../docs/reports/completed/2026-06-20-slice-go1-coverage-misc-implementation.md)

### 工时

实际 ~1.5h(预估 4h)。切片前小看了 Explore agent 报告,模式建立后批量补测效率高。

---

## 21:30~ — Go-2:logging 包补测

### 范围

logging 79.6% → ≥90%。

### 实施

- `internal/logging/handler_extra_test.go`(9 测试):NewLogger dev/prod/test 各模式 TextHandler/JSONHandler + level 屏蔽 + AddSource + SetDefault + FromContext 协同
- 不覆盖 `newID` crypto/rand fallback(OS 正常不会失败)

### 关键技巧

1. **stdout 重定向**:`os.Pipe()` + 暂存/恢复 `os.Stdout` 捕获 slog 输出
2. **slog.Default 清理**:每测试 defer 恢复,避免污染后续测试
3. **TextHandler vs JSONHandler 区分**:`strings.HasPrefix(out, "{")` 区分 JSON

### 实测覆盖率

| 函数 | 切片前 | 切片后 |
|---|---|---|
| NewLogger | 0% | 100% |
| newID | 75% | 75%(crypto/rand fallback 不覆盖,OS 正常不触发) |
| 整包 | 79.6% | **98.0%** 🟢 |

### 文档

- [spec](../docs/reports/completed/2026-06-20-slice-go2-coverage-logging-spec.md) + [implementation](../docs/reports/completed/2026-06-20-slice-go2-coverage-logging-implementation.md)

### 工时

实际 ~1h(预估 4h)。NewLogger 是单函数,stdout 重定向模式建立后批量测试快。

---

## 22:00~ — Go-3:hub 包补测(并发测试)

### 范围

hub 72.4% → ≥90%(并发难点,最高风险切片)。

### 实施

- `internal/hub/client_coverage_test.go`(12 测试):NewClient/Send/Close/writeLoop/RegisterClient/SendCommandToVisitor 全覆盖
- 核心:`newTestWSConnsPair` 用 httptest server + websocket.Accept/Dial 建立真实 *websocket.Conn 对
- 并发安全:TestSendCommandToVisitor_ConcurrentMultipleSenders(50 goroutine 并发)

### 关键修正

1. **InsecureSkipVerify 安全警告**:coder/websocket 的 InsecureSkipVerify 是 WS origin 校验(非 TLS),但安全工具警告名字。改用 `Origin: srv.URL` 精确匹配 server Host,默认严格 origin 检查通过
2. **close of closed channel panic**:cleanup 用 `sync.Once` 保护 close 调用
3. **Send 文档承诺**:Close 后 Send 不保证返回 false(只承诺"队列满返回 false"),改测试为先填满 writeCh 再 Close+Send

### 强制验证

`go test -race -count=3` 在并发测试上全绿,无 race 无 flaky。

### 实测覆盖率

| 函数 | 覆盖率 |
|---|---|
| NewClient/Send/Close/RegisterClient/SendCommandToVisitor | 100% |
| writeLoop | ~95%(Conn.Write 错误路径未 mock) |
| PublishEvent/SubscribeSession/UnsubscribeSession | 80-89%(subscriber channel 满丢弃路径) |
| **整包** | **94.1%** 🟢 |

### 文档

- [spec](../docs/reports/completed/2026-06-20-slice-go3-coverage-hub-spec.md) + [implementation](../docs/reports/completed/2026-06-20-slice-go3-coverage-hub-implementation.md)

### 工时

实际 ~2h(预估 8h)。httptest server + WS Dial 模式直接复用,比预估快。

---

## 22:30~ — Go-4:storage 适配器补测

### 范围

storage 57.6% → ≥90%(适配器全 0% 是大头)。

### 实施

- `internal/storage/adapters_coverage_test.go`(~530 行,~30 测试):4 个适配器 Connect/Ping/Close + Redis 操作层 + erasure ListEventBlobKeysBySessions
- 全 docker 集成:helperPGPool(已有)+ helperRedis/helperMinIO(新建,沿用 ConnectX 模式)

### 关键修正

1. `*redis.Client` vs `*storage.Redis`:helper 应返回包装层 *Redis 不是底层 Client
2. event_blobs schema:无 payload/content_type 列(有 started_at/ended_at/event_count/checksum_sha256)
3. Lua 关键字小写:`return` 不是 `RETURN`
4. config.Default() 不存在:直接构造 `&config.Config{...}`

### 实测覆盖率

**86.5%** 🟡(差 3.5pp 到 90% 目标)

| 函数 | 覆盖率 |
|---|---|
| ConnectRedis/Ping/Close + Redis 操作 | 75-100% |
| ConnectPostgres/Ping/Close | 84.6-100% |
| ConnectMinIO/Ping/PutBytes/GetBytes | 75-87.5%(Close 是 no-op 不统计) |
| Stores.Connect/Close/PingAll | ~80-100% |
| erasure_repo.ListEventBlobKeysBySessions | 84.6% |

### 未达目标说明

剩余 3.5pp gap:
- minio.Close 0%(no-op 空函数,工具不统计)
- 各 repo 函数 80-90%(scan 边缘分支,nil/invalid 数据)
- erasure DeleteVisitorByFingerprint 67.7%(多表 cascade 错误分支)

按 plan 风险点 #5"不为达 90% 写为测而测的弱断言测试",接受 86.5%。**storage 是 Go phase 唯一未达 90% 的包**,留 R&D backlog。

### 文档

- [spec](../docs/reports/completed/2026-06-20-slice-go4-coverage-storage-spec.md) + [implementation](../docs/reports/completed/2026-06-20-slice-go4-coverage-storage-implementation.md)

### 工时

实际 ~2.5h(预估 12h)。适配器测试模式直接复用 helperPGPool + ConnectX 模式,比预估快。

---

## 跨日 2026-06-21 — Go-5 recording + Go-6 api

### Go-5 recording(48%→77.7%)

详见 [daily/2026-06-21](./2026-06-21.md)。

### Go-6 api(38.2%→47.9%,Commit 1 完成)

- 30 个新测试覆盖纯函数(ternary/isURLAllowed/eventCountOf/forEachEventPayload/extractFullSnapshotEnvelope/buildCommandPayload/devHint)
- 7 个构造函数 + newStaticHandler + health handlers
- Commit 2 WS handlers 留 R&D backlog

详见 [Go-6 impl](../docs/reports/completed/2026-06-21-slice-go6-coverage-api-implementation.md)。

